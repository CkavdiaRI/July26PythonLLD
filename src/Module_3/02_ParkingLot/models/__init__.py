# It marks a directory as a Python package, allowing the modules within it to be imported.
# It lets us write -> from models.gate import Gate instead of dealing with raw paths.

# you can also use this file to define package-level variables, functions, or classes
# that should be accessible when the package is imported.

# you can use it to run setup code which is executed when the package is imported.
# This can be useful for initializing package-level resources or configurations.